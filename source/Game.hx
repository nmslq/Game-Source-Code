package;

import Flx;

public static function onGameStart(){
   flx()
   mod.command()
   if windows{
     set.window.name(game)
     flx.getPath(files)
     file.getPath()
     getPath.exites()
   }
   if android{
     clean.system()
     system.filePath()
     file.getPath.exites()
     android.flx.getPath(files)
     flx.getPath.exites()
   }
}
