function onGameStart()
   Flx()
   if Windows then
     set.win.name(game)
     flx.getPath(files)
     getPath.exites()
   end
   if Android then
     clean.sys()
     sys.rePath()
     android.flx.getPath(files)
     flx.getPath.exites()
   end
end
